class Solution {
    public boolean squareIsWhite(String coordinates) {
        
        int ans = (coordinates.charAt(1)-48)+(coordinates.charAt(0)-97);
        
        return ans%2==0 ? true : false;
    }
}
